/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['https://sayed3040.sobhoy.com'], // Add the domain(s) where your images are hosted
    },
  };
  
  export default nextConfig;