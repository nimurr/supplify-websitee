import ProtocolPage from '@/Dashboard/sidebarMenu/Protocol';
import React from 'react';

const page = () => {
    return (
        <div>
              <ProtocolPage />
        </div>
    );
};

export default page;