import SignUpNext from '@/components/auth/SignNext';
import React from 'react';

const page = () => {
    return (
        <div>
            <SignUpNext />
        </div>
    );
};

export default page;