import SignupSuccess from '@/components/auth/SignUpSuccess';
import React from 'react';

const page = () => {
    return (
        <div>
            <SignupSuccess />
        </div>
    );
};

export default page;